{
  "name": "agent-w",
  "version": "0.4.0",
  "description": "Agent W - Tools and skills for Model W projects with coding agents",
  "author": {
    "name": "with-madrid.com",
    "url": "https://github.com/ModelW/agent-w"
  },
  "skills": "./skills",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -d .agents/skills ]; then mkdir -p .claude/skills && for d in .agents/skills/*; do if [ -d \"$d\" ]; then ln -sf \"$(pwd)/$d\" \".claude/skills/$(basename \"$d\")\"; fi; done; fi"
          }
        ]
      }
    ]
  }
}
